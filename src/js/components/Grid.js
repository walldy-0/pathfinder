class Grid {
  constructor(wrapper) {
    const thisGrid = this;

    thisGrid.render(wrapper);

    console.log(thisGrid.dom);
  }

  render(wrapper) {
    const thisGrid = this;

    thisGrid.dom = {};
    thisGrid.dom.wrapper = wrapper;
  }

  insertTable() {
    const thisGrid = this;


  }

  insertRow() {
    const thisGrid = this;

  }

  insertCell(row) {
    const thisGrid = this;

  }

  createTable() {
    const thisGrid = this;

    
  }
}

export default Grid;